# Debug Log — 2026-05-15

## TODO — OER workflow opens then freezes the viewer

### 现象
- CatBot 一次性生成 OER workflow → 浏览器 Reload (或 Windows 端打开 localhost:3100) → UI 完全卡死,F12 都开不出来。
- WSL 侧 `vite` (:3100) 与 FastAPI (:8000) 两个进程都健康,CPU 占用正常,所以**死循环在浏览器主线程**,不是后端。
- Playwright 干净 profile 加载 `localhost:3100/` 完全不卡(8 秒就绪,console 干净)→ **必须有用户持久化状态**才会触发。
- 客户端 workflow 持久化层是 `sql.js` WASM SQLite,落在 IndexedDB(`src/lib/api/workflow.ts:20`)。

### 推测根因
跟刚修过的 PR #53 (`trajectory-bond-cache.svelte.ts` 自触发 `$effect`) **同类问题**:WorkflowEditor 里至少 10 个 `$effect` 监听 `nodes` / `edges` / `node_statuses` / `selected_node`,其中某个或多个 effect 读 + 写同一份 reactive state,在 OER 这种"分支多 + 共享自由能节点"的拓扑下依赖图不收敛 → Svelte 报 `effect_update_depth_exceeded` 把主线程榨干。

线性 workflow (HER 之类) 节点少,环 settle 得快,绕开了边界条件;OER 刚好踩中。

### 待办
- [ ] **复现一份炸 UI 的 OER workflow JSON**。两种路径:
  - 让用户 Export 工具栏存到本地之后再发我 (前提是 UI 还能动)
  - 我用 Claude / Gemini provider 自己跑一次 CatBot 复现
- [ ] 用 Playwright 加载这份 graph,捕获真正的 `pageerror` 堆栈
- [ ] 定位具体哪个 `$effect` 自触发,按 PR #53 手法处理(把"读 X + 写 X"改成闭包局部变量缓存)
- [ ] 顺手审一遍 `WorkflowEditor.svelte` 的所有 `$effect` (line 197 / 303 / 309 / 1632 / 1649 / 1669 / 1934 / 1952 / 1963 / 1966),看是否还有同类隐患

### 临时规避
- CatBot 生成完工作流别立刻刷新,先 Export workflow JSON 备份
- OER 分批生成(slab → O → OH → OOH),每步保存
- 真炸了:Application → Storage → Clear site data
