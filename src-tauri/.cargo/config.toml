[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[env]
# Use system pkg-config instead of conda's, which mangles include paths
PKG_CONFIG = "/usr/bin/pkg-config"
