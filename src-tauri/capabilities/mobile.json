{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "iOS-only permissions. Scoped to iOS so desktop (and Android, where the plugins aren't compiled) capability validation never sees ios-speech / bg-grace.",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": ["ios-speech:default", "bg-grace:default"]
}
