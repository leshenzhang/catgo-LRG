# Build artifacts + Tauri-generated bindings — all regenerated by `cargo build`
# / `tauri ios` (mirrors how tauri-plugin-bg-grace's .tauri/ stays untracked).
# Only the hand-authored source + permissions/default.toml are committed.
/.tauri/
/Cargo.lock
/ios/.build/
/ios/Package.resolved
/permissions/autogenerated/
/permissions/schemas/
